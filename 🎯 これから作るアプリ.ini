🎯 これから作るアプリ

AI旅行提案アプリ（OpenAI搭載 / 日本語UI）

機能：

行き先、日数、テーマを入力

「提案して！」ボタンでChatGPTが旅プランを生成

結果は「1日目」「2日目」…の構成で表示

エラー時は日本語メッセージ

🧱 ステップ1：プロジェクト準備（Cursor内）

Cursorを開いて、
「New Project」→「HTML + JS」 を選択。

ファイル構成を以下のようにする：

/AITravelPlanner
├── index.html
├── script.js
└── .env


.env に以下を入れる（OpenAIキーは自分のを）：

OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxx

💻 ステップ2：index.html

これを貼る👇
（シンプルだけどUI綺麗・スマホ対応）

<!DOCTYPE html>
<html lang="ja">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<title>AI旅行プランナー</title>
<link rel="icon" href="https://cdn-icons-png.flaticon.com/512/854/854878.png" />
<style>
  body {
    font-family: "Hiragino Sans", sans-serif;
    background: linear-gradient(180deg, #fefefe, #e6f3ff);
    padding: 2em;
    text-align: center;
  }
  h1 { font-size: 1.6em; margin-bottom: 1em; }
  input, textarea, button {
    margin: 0.5em 0;
    padding: 0.7em;
    width: 90%;
    max-width: 400px;
    border: 1px solid #ccc;
    border-radius: 8px;
    font-size: 1em;
  }
  button {
    background: #007bff;
    color: #fff;
    cursor: pointer;
    transition: 0.3s;
  }
  button:hover { background: #0056b3; }
  #result {
    margin-top: 2em;
    white-space: pre-wrap;
    text-align: left;
    background: #fff;
    padding: 1em;
    border-radius: 10px;
    box-shadow: 0 0 10px rgba(0,0,0,0.1);
  }
</style>
</head>
<body>
  <h1>🌏 AI旅行プランナー</h1>
  <input id="destination" placeholder="行き先（例：京都）" />
  <input id="days" placeholder="日数（例：2泊3日）" />
  <input id="theme" placeholder="テーマ（例：温泉と歴史）" />
  <button onclick="generatePlan()">提案して！</button>
  <div id="result"></div>

  <script src="script.js"></script>
</body>
</html>

⚙️ ステップ3：script.js

ここでAI呼び出しを組み込む👇

async function generatePlan() {
  const destination = document.getElementById("destination").value || "京都";
  const days = document.getElementById("days").value || "2泊3日";
  const theme = document.getElementById("theme").value || "癒やしと美食";

  const prompt = `
  あなたは日本語で話す旅行プランナーです。
  以下の条件で、日ごとにわかりやすく旅の提案を作成してください。
  - 行き先：${destination}
  - 日数：${days}
  - テーマ：${theme}
  - 構成：1日目（午前・午後・夜）／2日目／最終日 のように記載
  - 最後に「この旅の魅力まとめ」も加えてください。
  `;

  const resultDiv = document.getElementById("result");
  resultDiv.innerText = "AIが旅を考えています…🧭";

  try {
    const response = await fetch("https://api.openai.com/v1/chat/completions", {
      method: "POST",
      headers: {
        "Content-Type": "application/json",
        "Authorization": `Bearer ${import.meta.env.OPENAI_API_KEY}`
      },
      body: JSON.stringify({
        model: "gpt-4o-mini",
        messages: [{ role: "user", content: prompt }]
      })
    });

    const data = await response.json();
    const text = data.choices?.[0]?.message?.content || "プラン生成に失敗しました。";
    resultDiv.innerText = text;

  } catch (error) {
    console.error(error);
    resultDiv.innerText = "通信エラーが発生しました。APIキーを確認してください。";
  }
}
